# ドキュメント運用

開発フロー全体（issue起票〜マージ）は `.claude/skills/issue-mr-flow/SKILL.md` を参照する
（唯一の実装フロー定義）。本ファイルはドキュメントの置き場所・ライフサイクルの参照表であり、
手順そのものは記載しない。

情報の寿命（ライフサイクル）と「誰のためのドキュメントか」で置き場所を切り分ける。

| ファイル | 対象 | 寿命 | 内容 | 運用 |
|---|---|---|---|---|
| `CLAUDE.md` / `.claude/rules/*.md` | AI専用 | 永続 | 規約・構成 | ルート直下・`.claude/rules/`。セッション開始時にAIが自動読込する。実装フローそのものは `.claude/skills/issue-mr-flow/SKILL.md` を参照。 |
| `plans/*.md` | 人間＋AI | 生成時点のスナップショットとして永続 | AIエージェントのplanモードが出力する方針・比較検討した案・承認記録 | タスクごとに自動生成され、人間の承認をもって確定する。手動で使い回したり中身を空にしたりせず、そのままコミットして履歴に残す。 |
| `HANDOFF.md` | 人間＋AI | 短期（常に最新状態のみ） | `.claude/skills/issue-mr-flow/SKILL.md`の23ステップに対応する進捗チェック表／現在地／次にやること／判断を迷った内容／未解決の内容／守るべき条件・触ってはいけない範囲 | Git管理下に置く。作業が終わるごとに現在状況を更新する。詳細な試行錯誤は書かず `worklog/` に逃がす。タスク完了・PR作成時（設計反映）に次のタスクへ向けてリセットする。 |
| `worklog/日付_<planファイル名>.md` | AI専用（人間も参照可） | ブランチ単位（PR作成前の設計反映で削除） | 「何を試した／うまくいった／ダメだったか」の詳細ログ | planモード完了時に作成し、同じセッションで作業する間に作業の節目ごとに頻繁に書き足す。PR作成前の設計反映でspec/ddrへ反映したうえで削除し、削除自体もコミットに含める。`.gitignore`には加えない（ブランチ上のコミット履歴として残すため）。squash mergeにより、mainには残さない。 |
| `docs/spec/機能名.md` | 人間＋AI | 永続（最新状態） | 背景・目的／仕様／影響範囲／設定項目／未決定事項・懸念点 | 「現在の正史」。実装完了のたびに最新仕様へ上書きする。新規作成時の人間承認は必須。plans／worklogの内容をMR作成時に反映する。 |
| `docs/ddr/000N-タイトル.md` | 人間＋AI | 永続（不変） | 「〇〇を検討したが✕✕を採用した」という意思決定の背景・却下案（DDR: Design Decision Record。architectureに限らない意思決定を対象とする） | 連番で管理し、一度マージしたら追記のみ（変更不可）。`docs/spec` の未決定事項が解消したら記録し、spec側の該当項目は削除してよい。plans／worklogの内容をMR作成時に反映する。 |

`HANDOFF.md` は空でも各見出し（現在地／次回やること等）だけを残した状態でルート直下に存在させておき、使うときに埋める運用とする（都度新規作成はしない）。

`HANDOFF.md`の「フロー進捗状況」表（`.claude/skills/issue-mr-flow/SKILL.md`の23ステップに対応）で、
「〜を合意まで繰り返す」と書かれたループ扱いのステップ（例: 7〜8, 11〜15, 16〜20）は、
レビュー往復が複数回になった場合、往復1回につき`[]`を1つ追加して進捗を表現する
（例: 3回目の往復が進行中なら`[x][x][]`のように、完了済みの回数分`[x]`を並べ、現在進行中／
未着手の回に`[]`を1つ足す）。同じループ範囲内のステップは常に同じ個数の`[]`を持つ
（例: 14と15は常に同数）。ループでない単発ステップ（1, 2, 3, 6, 9等）は`[]`/`[x]`を1つのみ持つ。
